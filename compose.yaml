services:
  db-adv-space:
    image: 'postgres:latest'
    restart: always
    ports:
      - 5432:5432
    environment:
      POSTGRES_DB: adv-space
      POSTGRES_PASSWORD: pass
      POSTGRES_USER: myuser
